# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Local only — never push to GitHub
scripts/
working/

# Trip data — managed separately from code deploys.
# Never commit this. Production data lives on the deployed site;
# a git push would overwrite it. Update deliberately via Netlify Drop.
public/trip.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
